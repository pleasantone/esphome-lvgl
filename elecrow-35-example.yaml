esphome:
  name: crowpanel-35-test
  friendly_name: CrowPanel 3.5" Test

substitutions:
  home_page: lighting_1

packages:
  common: !include common.yaml
  device: !include devices/DIS05035H.yaml
  layout: !include layouts/480x320.yaml
  # optional features -- list them after layout:
  # idle: !include features/idle/idle.yaml                # dim, go home, sleep
  # ceiling: !include features/idle/sun.yaml              # brightness follows the sun
  # sleep_clock: !include features/sleep_clock/sleep_clock.yaml
  # memory: !include features/diagnostics/memory.yaml     # heap sensors, for chasing a leak
