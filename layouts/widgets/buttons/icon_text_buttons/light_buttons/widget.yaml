# required vars:
# - uid
# - entity_id
# - width
# - height
# - text

# button:
  <<: !include
    file: themed.yaml
    vars:
      uid: ${uid}
      entity_id: ${entity_id}
      width: ${width}
      height: ${height}
      text: ${text}
      icon_on: "\U000F06E8"
      icon_off: "\U000F0336"
